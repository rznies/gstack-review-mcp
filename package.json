{
  "name": "gstack-review-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "embed-skills": "bash scripts/embed-skills.sh",
    "test": "bun test",
    "postinstall": "if [ \"$SKIP_POSTINSTALL\" != \"1\" ]; then bun run build; else echo \"Skipping postinstall build because SKIP_POSTINSTALL=1\"; fi",
    "build": "bash scripts/embed-skills.sh && bun build src/index.ts --outdir dist --target bun",
    "start": "bun ./dist/index.js"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^4.0.1"
  },
  "devDependencies": {
    "bun-types": "^1.3.10"
  }
}
